#include "scene.h"
#include "utils.h"

t_entity	validate_file_type(char *file)
{
	// fatal_error(NULL, "invalid file format");
}

void	validate_entity(t_context *ctx, t_entity entity)
{
	(void)entity;
	// fatal_error(ctx, "invalid scene description");
}
